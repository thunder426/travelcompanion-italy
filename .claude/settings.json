{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" 2>/dev/null && jq -r '.tool_input.file_path // empty' | grep -qE '/tasks/[^/]+\\.md$' && [ -f tasks/build_tasks_html.py ] && python3 tasks/build_tasks_html.py >/dev/null 2>&1; exit 0"
          }
        ]
      }
    ]
  }
}